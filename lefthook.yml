pre-commit:
  parallel: true
  commands:
    fmt:
      run: just check-fmt
    lint:
      run: just check-lint
    typos:
      run: just check-typos

commit-msg:
  commands:
    conventional-commit:
      run: bash scripts/check-commit-msg.sh {1}
