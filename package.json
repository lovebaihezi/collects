{
  "name": "test",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "build:web": "trunk build --release",
    "build:web:preview": "CARGO_FEATURES=preview trunk build --release",
    "dev": "cargo r",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "typescript": "^5.5.2",
    "wrangler": "^4.25.1"
  }
}
