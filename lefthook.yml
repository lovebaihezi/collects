pre-commit:
  parallel: true
  commands:
    fmt:
      run: just check-fmt
    lint:
      run: just check-lint
    typos:
      run: just check-typos
    migration-check:
      run: just scripts::migration-check
      glob: "services/migrations/*.sql"
    ast-grep:
      run: sg scan
